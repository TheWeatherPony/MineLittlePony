{
    "name": "${name}",
    "mcversion": "${mcversion}",
    "version": "${version}",
    "revision": "${revision}",
    "author": "${author}",
    "description": "${description}",
    "description.minelittlepony": "${description_minelittlepony}",
    "description.hdskinsmod": "${description_hdskinsmod}",
    "mixinConfigs": [
        "minelp.mixin.json",
        "hdskins.mixin.json"
    ]
}
